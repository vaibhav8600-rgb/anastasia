# Anastasia (Anna) — local voice assistant
# Python 3.10+ (tested on 3.13)

# GUI (web frontend in an Edge WebView2 window)
pywebview>=5.0

# Core
pydantic>=2.5
requests>=2.31
rapidfuzz>=3.9

# Voice input + STT
sounddevice>=0.4.6
numpy>=1.26
faster-whisper>=1.0        # downloads the Whisper model on first use (CPU, int8)
piper-tts>=1.4.2           # official OHF Piper runtime
websocket-client>=1.7      # optional: Deepgram streaming STT (stt_mode=streaming)
google-genai>=2.10         # optional: Gemini Live premium engine (engine_mode=gemini_live)

# Computer control
pyautogui>=0.9.54
pyperclip>=1.8
Pillow>=10.0               # screenshots
keyboard>=0.13.5           # global push-to-talk hotkey

# Dev / tests
pytest>=8.0

# Optional — uncomment if you want wake-word mode ("Hey Jarvis"):
# openwakeword>=0.6

# Optional — natural Kokoro voice (also download model + voices in Settings):
# kokoro-onnx>=0.5

# Optional — local OCR so Anna can READ your screen with nothing leaving the
# PC (11B). Needs the Tesseract binary as well:
#   winget install UB-Mannheim.TesseractOCR
# pytesseract>=0.3.10

# App control (11C). UIA drives native Windows apps; Playwright drives the
# browser (attaches to YOUR running Chrome/Edge over CDP — no bundled
# Chromium download needed, so skip `playwright install`):
uiautomation>=2.0.20       # native Windows control tree (UIA)
playwright>=1.40           # browser DOM control over the DevTools Protocol
